---
sidebar_position: 2
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import LanguageStrip from '@site/src/components/LanguageStrip';

# Instalación del SDK

Contamos con librerías oficiales para los siguientes lenguajes:

<LanguageStrip />

Si usas algún otro lenguaje, aún puedes consumir la API usando tu propio cliente HTTP.

## Usando una de nuestras librerías oficiales

Empieza por incluir el cliente de Facturapi en las dependencias de tu proyecto.

<Tabs groupId="codeExamples">
<TabItem value="js" label="Node.js" default>

Instala el paquete de [NPM](https://www.npmjs.com/package/facturapi).

```bash
$> npm install --save facturapi
```

</TabItem>
<TabItem value="cs" label="C#">

Instala el
[paquete de NuGet](https://www.nuget.org/packages/Facturapi)
desde Visual Studio, o bien, desde la terminal del Package Manager:

```bash
dotnet add package Facturapi
```

</TabItem>
<TabItem value="java" label="Java">

Instala el [paquete de Maven Central](https://central.sonatype.com/artifact/io.facturapi/facturapi-java).
Elige el bloque que corresponda con tu herramienta de construcción.

Usando Maven:

```xml
<dependency>
  <groupId>io.facturapi</groupId>
  <artifactId>facturapi-java</artifactId>
  <version>1.0.0</version>
</dependency>
```

Usando Gradle:

```gradle
implementation("io.facturapi:facturapi-java:1.0.0")
```

</TabItem>
<TabItem value="php" label="PHP">

Importa el [paquete de Packagist](https://packagist.org/packages/facturapi/facturapi-php) usando composer:

```bash
$> composer require "facturapi/facturapi-php"
```

</TabItem>
</Tabs>

Importa la librería antes de usarla.

<Tabs groupId="codeExamples">
<TabItem value="js" label="Node.js" default>

CommonJS
```javascript
import Facturapi from 'facturapi'
```

ESM / Typescript
```javascript
import Facturapi from 'facturapi';
```

</TabItem>
<TabItem value="cs" label="C#">

```csharp
using Facturapi;
```

</TabItem>
<TabItem value="java" label="Java">

```java
import io.facturapi.Facturapi;
```

</TabItem>
<TabItem value="php" label="PHP">

```php
require_once __DIR__ . '/vendor/autoload.php';

use Facturapi\Facturapi;
```

Si tu framework ya carga el autoload de Composer automáticamente, puedes omitir `require_once`.

</TabItem>
<TabItem value="curl" label="cURL">

cURL no requiere de instalar un cliente adicional

</TabItem>
</Tabs>

## Usando tu propio cliente HTTP

Si consumes la API directamente usando tu propio cliente HTTP, no requieres
instalar nada. Puedes avanzar a la siguiente sección.
